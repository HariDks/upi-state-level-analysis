# Raw source data — never versioned. Pulled fresh into data/raw/ during Phase 1.
data/raw/

# Regenerable analysis outputs
output/

# R session
.Rhistory
.RData
.Rapp.history
.Ruserdata
*.Rproj.user/

# macOS
.DS_Store

# LaTeX build artefacts
paper/**/*.aux
paper/**/*.log
paper/**/*.out
paper/**/*.toc
paper/**/*.bbl
paper/**/*.blg
paper/**/*.synctex.gz
paper/**/*.fdb_latexmk
paper/**/*.fls

# renv manages its own .gitignore inside renv/ after renv::init()

# Private/local-only files (not for the public repo)
Thesis docx.docx
Total UPI States-2021/

# Claude Code project instructions — local-only, not for the public repo
CLAUDE.md

# Internal learning journal — local-only
learning day 1.md
